import type { CSSProperties } from "react";
import { theme } from "./theme";

export const st: Record<string, CSSProperties> = {
  app: {
    minHeight: "100vh",
    background: theme.bg,
    color: theme.textPrimary,
    fontFamily: theme.fontSans,
    maxWidth: 640,
    margin: "0 auto",
  },
  hdr: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
    padding: "12px 12px 5px",
    borderBottom: `1px solid ${theme.bgSubtle}`,
  },
  title: { fontSize: 15, fontWeight: 700, color: "#f3f4f6", letterSpacing: "-0.02em" },
  sub: { fontSize: 9, color: theme.textFaint, letterSpacing: "0.03em", textTransform: "uppercase" },
  yrBadge: {
    fontSize: 10,
    padding: "3px 8px",
    borderRadius: 14,
    background: theme.border,
    color: theme.info,
    cursor: "pointer",
    border: `1px solid ${theme.borderSubtle}`,
  },
  nav: {
    display: "flex",
    gap: 1,
    padding: "5px 6px",
    overflowX: "auto",
    borderBottom: `1px solid ${theme.bgSubtle}`,
    WebkitOverflowScrolling: "touch",
  },
  navBtn: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    gap: 1,
    padding: "5px 6px",
    border: "none",
    background: "transparent",
    color: theme.textFaint,
    borderRadius: 7,
    cursor: "pointer",
    flex: 1,
    minWidth: 42,
  },
  navAct: { background: theme.border, color: theme.textPrimary },
  main: { padding: 10, paddingBottom: 40 },
  card: { background: theme.bgCard, borderRadius: 10, padding: 12, border: `1px solid ${theme.border}` },
  secT: { fontSize: 13, fontWeight: 600, color: theme.textSecondary, marginBottom: 4 },
  col: { display: "flex", flexDirection: "column", gap: 10 },
  kpiRow: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 6 },
  kpiCard: {
    background: theme.bgCard,
    borderRadius: 10,
    padding: 10,
    display: "flex",
    flexDirection: "column",
    gap: 1,
    border: `1px solid ${theme.border}`,
  },
  propC: { background: theme.bgCard, borderRadius: 10, padding: 10, border: `1px solid ${theme.border}` },
  editRow: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
    padding: "7px 4px",
    borderBottom: `1px solid ${theme.border}`,
    gap: 5,
  },
  edVal: {
    color: theme.info,
    fontSize: 12,
    cursor: "pointer",
    fontFamily: theme.fontMono,
    padding: "3px 6px",
    borderRadius: 6,
    background: theme.bgSubtle,
    whiteSpace: "nowrap",
  },
  inp: {
    padding: "5px 7px",
    borderRadius: 6,
    border: "1px solid #374151",
    background: theme.bg,
    color: theme.textPrimary,
    fontSize: 12,
    fontFamily: theme.fontMono,
    outline: "none",
    boxSizing: "border-box",
  },
  miniBtn: { padding: "3px 8px", borderRadius: 5, border: "none", background: "#166534", color: "#86efac", cursor: "pointer", fontSize: 12 },
  addBtn: {
    padding: "3px 10px",
    borderRadius: 7,
    border: "1px solid #374151",
    background: theme.border,
    color: theme.textSecondary,
    cursor: "pointer",
    fontSize: 15,
    lineHeight: "1",
  },
  smBtn: {
    padding: "4px 10px",
    borderRadius: 7,
    border: "1px solid #374151",
    background: theme.bgSubtle,
    color: theme.textSecondary,
    cursor: "pointer",
    fontSize: 11,
  },
};
